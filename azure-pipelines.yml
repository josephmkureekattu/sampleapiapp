# Docker

# Build a Docker image
# https://docs.microsoft.com/azure/devops/pipelines/languages/docker

trigger:
- master

resources:
- repo: self

variables:
  tag: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: Build image
  jobs:
  - job: Build
    displayName: Build
    pool:
      vmImage: ubuntu-latest
    steps:
    - task: Docker@2
      inputs:
        command: 'login'
        arguments: '--username josephm101 --password dckr_pat_lH1FI6GOlBn1Q9GUpxWjkCeGl24'
    - task: Docker@2
      inputs:
        command: 'build'
        Dockerfile: '**/Dockerfile'